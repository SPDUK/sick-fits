import React, { Component } from 'react';
import { Query } from 'react-adopt';

export default class Items extends Component {
  render() {
    return (
      <div>
        <p>Items!</p>
      </div>
    );
  }
}
